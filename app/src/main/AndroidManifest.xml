<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.localdiskieapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/localdiskielogoe"
        android:label="@string/app_name"
        android:roundIcon="@drawable/localdiskielogoe"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalDiskieApp">
        <activity android:name=".createlineup"></activity>
        <activity android:name=".phoneVarification" />
        <activity android:name=".facebookregister" />
        <activity android:name=".Register" />
        <activity android:name=".UpdateScore" />
        <activity android:name=".SetFicture" />
        <activity android:name=".LiveScore" />
        <activity android:name=".localDiskieHome" />
        <activity android:name=".profile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />

        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>